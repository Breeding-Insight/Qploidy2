% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/call_dosages.R
\name{call_BAF_dosages}
\alias{call_BAF_dosages}
\title{Assign BAF Dosages Using Selected Model}
\usage{
call_BAF_dosages(
  baf_vec,
  selected_model = NULL,
  bw = NULL,
  dist = NULL,
  add_uniform = NULL,
  uniform_weight = NULL,
  cn = NULL,
  max_cn = NULL,
  plot = FALSE
)
}
\arguments{
\item{baf_vec}{Numeric vector of BAF values (in [0,1]).}

\item{selected_model}{Object of class 'selected_BAF_model' (output of select_best_baf_model). If NULL, user must provide bw, dist, add_uniform, uniform_weight, and cn.}

\item{bw}{Bandwidth (SD or concentration) for kernel smoothing (required if selected_model is NULL).}

\item{dist}{Distribution type for template peaks: "gaussian", "beta", "beta_binomial", or "negative_binomial" (required if selected_model is NULL).}

\item{add_uniform}{Logical. Whether to add a uniform noise component (required if selected_model is NULL).}

\item{uniform_weight}{Numeric in [0,1]. Mixture weight for uniform component (required if selected_model is NULL and add_uniform is TRUE).}

\item{cn}{Integer. Copy number (required if selected_model is NULL).}

\item{max_cn}{Integer or NULL. When provided, the output data.frame will include columns
\code{dosage_0} through \code{dosage_{max_cn}}, with \code{NA} for dosages exceeding \code{cn}.
Useful for aligning outputs across markers with different CN values.}

\item{plot}{Logical. If TRUE, generates a plot of BAF values colored by maximum probability.}
}
\value{
A list with element \code{data}: a data.frame with columns \code{BAF}, \code{dosage}
  (integer), \code{max_prob} (probability), and per-dosage probability columns
  \code{dosage_0} through \code{dosage_{cn}} (or \code{dosage_{max_cn}} when \code{max_cn} is
  provided, with \code{NA} for impossible dosages). If \code{plot=TRUE}, also includes element
  \code{plot}.
}
\description{
Given a vector of BAF values and a selected_BAF_model object (from select_best_baf_model),
assigns the most likely dosage (0:CN) to each BAF value, and returns the dosage probabilities.
}
